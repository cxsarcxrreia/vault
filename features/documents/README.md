# Documents Feature

This folder owns lightweight document metadata and external links.

Documents should point to Google Drive or another external URL. Do not add heavy file hosting in v1.

Each document should carry a simple `phase_key` so project pages can group onboarding, proposal/scope, creative direction, production, deliverables, and general references without creating standalone phase workspaces.
